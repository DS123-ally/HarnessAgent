# Coding Agent

Use this skill when working on HarnessAgent source code.

- Read the relevant files before making claims.
- Prefer small edits that match the existing Python style.
- Run `uv run python -m unittest discover -s tests` after production code changes.
- Use file tools for source inspection and edits.
- Use durable memory only for facts that should matter across sessions.
- Use tasks when work has multiple steps or follow-up items.
