# HarnessAgent

This project implements a coding agent framework capable of interacting with the file system and executing commands.

## Project Structure
The core logic resides in `main.py`, utilizing tools defined within the `forge` package.

## New Feature Added
A simple command-line calculator, `calculator.py`, has been added to the project root for basic arithmetic operations.